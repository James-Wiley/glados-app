import 'package:flutter/material.dart';

class GlovePage extends StatelessWidget {
  const GlovePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Glove Page')));
  }
}
